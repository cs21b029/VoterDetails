Steps to Run :--
1) Open the terminal in the project directory and run Command "npm install"
2) now run the Command "json-server db.json --port 3001"
3) now open a new terminal in the same directory
4) run Command "npm start"
5) you will be redirected to website.
